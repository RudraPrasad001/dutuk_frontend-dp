import { StyleSheet } from "react-native";

const authEmailPageStyle = StyleSheet.create({
  container: {
    flex: 1,
  },
  inputSection: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
  },
  headerText: {
    fontSize: 30,
    marginTop: 20,
    marginLeft: 35,
  },
});

export default authEmailPageStyle;

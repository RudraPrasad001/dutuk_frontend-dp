import { StyleSheet } from "react-native";

const authLableStyle = StyleSheet.create({
  label: {
    fontWeight: "bold",
    alignSelf: "flex-start",
    marginLeft: 40,
    marginBottom: 5,
    marginTop: 10,
  },
});

export default authLableStyle;

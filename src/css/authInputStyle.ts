import { StyleSheet } from "react-native";

const authInputStyle = StyleSheet.create({
  inputField: {
    borderColor: "gray",
    borderWidth: 2,
    borderRadius: 10,
    padding: 10,
    marginBottom: 15,
    width: 300,
  },
});

export default authInputStyle;

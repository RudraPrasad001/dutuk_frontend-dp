import React from "react";
import { Text, View } from "react-native";

const settings = () => {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text style={{ fontWeight: "bold", fontSize: 50 }}>Settings</Text>
    </View>
  );
};

export default settings;
